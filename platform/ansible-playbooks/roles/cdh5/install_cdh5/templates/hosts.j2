127.0.0.1               localhost.localdomain localhost
::1             localhost6.localdomain6 localhost6
{% for host in groups['all_servers'] %}
{{ hostvars[host]["ansible_default_ipv4"]["address"] }}    {{ host }} {{ host }}.localdomain
{% endfor %}
